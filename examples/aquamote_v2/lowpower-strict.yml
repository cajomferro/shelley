name: LowPower
operations:
    setup:
        start: True
        next: [sleep]
    cancel:
        next: [setup]
        start: True
    sleep:
        next: [wakeup, cancel]
        start: false
    wakeup:
        next: [setup]
        start: false
