device:
  name: Timer
  events: [started, canceled, timeout] # started is start event
  behavior:
    - [started, canceled]
    - [started, timeout]
    - [canceled, started]
    - [timeout, started]
