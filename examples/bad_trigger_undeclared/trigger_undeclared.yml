device:
  name: WrongButton
  behavior:
    - [on, on]
  components:
    b: Button
  events:
    on:
        micro: [ b.press, b.release]
    off:
        micro: [ b.press, b.release] # ERROR: off is undeclared!
