device:
  name: WrongButton
  behavior:
    - [on, on]
  components:
    b: Button
  events:
    - on:
        micro: [ b.pressed, b.released]
    - off:
        micro: [ b.pressed, b.released] # ERROR: off is undeclared!
