name: LowPowerStrict
start_with: [setup, cancel]
end_with: $ANY
operations:
    setup:
        next: [sleep]
    cancel:
        next: [setup]
    sleep:
        next: [wakeup, cancel]
    wakeup:
        next: [setup]
