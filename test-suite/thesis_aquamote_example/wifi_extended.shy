base WiFi {
 initial enable -> connect ;
 final disable -> enable ;
 connect -> connect_ok, connect_error ;
 connect_ok -> get_rssi, disable ;
 connect_error -> disable ;
 get_rssi -> get_rssi, disable ;

}