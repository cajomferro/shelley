device:
  name: WrongButton
  behavior:
    - [on, off]
  components:
    b: Button
  events:
    - on:
        micro: [ b.pressed]
    # - off:
    #      micro: [ b.released] # ERROR: off is missing!
