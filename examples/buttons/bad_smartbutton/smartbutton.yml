device:
  name: SmartButton
  behavior:
    - [on, on]
  components:
    b: SingleClickButton
  events:
    - on:
        start: True
        micro: [ b.pressed, b.pressed] # this is not an allowed sequence for SingleClickButton
