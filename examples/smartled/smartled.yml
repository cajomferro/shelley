name: SmartLed
start_with: [active]
end_with: [standby]
components:
  l: Led
  t: Timer
operations:
  active:
    next: [standby]
    micro:
      - [l.on, t.start]
  standby:
    next: [active]
    micro: 
      - xor:
        - [t.timeout, l.off]
        - [l.off, t.cancel]
