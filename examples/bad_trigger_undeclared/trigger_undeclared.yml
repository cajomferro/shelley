name: WrongButton
start_with: $ANY
end_with: $ANY
components:
    b: Button
operations:
    on:
        next: [on]
        micro: [ b.press, b.release]
    off:
        next: []
        micro: [ b.press, b.release] # ERROR: off is undeclared!
